# Input provenance

Supplied archive: `FindExtension.zip`, containing one `FindExtension.nb` notebook.
The archive was inspected without evaluating the notebook. The notebook text
identifies Wolfram front end version 15.0; that does not establish runtime
compatibility or validation of the new package.

SHA-256 of supplied archive:
`381e80382e8736aa00e18d8fd48c6f391b9ba0b15e80ff7621e7589147174930`

SHA-256 of notebook (986737 bytes):
`d70951fdb88128556ee7ea762652413b6f8b5e970c4437c2f3293d9b07eac007`

`notebook_inputs.txt` and `notebook_inputs.json` contain 220 input-cell inspections
produced by `tools/extract_notebook.py`. Source lines are original textual
notebook line numbers. The original also contains output cells, graphics,
notebook metadata and interactive state that the extraction does not reproduce.
The `collect` definition, for example, occurs in displayed information output,
not as a self-contained input definition in the extract.

These extracts are NOT executable WL source. Special boxes and partial scratch
inputs can retain approximate display syntax. They are included for audit
and navigation, not as a reconstructed, trusted notebook program.

The source question is https://mathematica.stackexchange.com/q/34011 .
The original user-provided notebook/extracts are not relicensed by this release.
The new algorithms and article are a separate work; classical Galois/Kummer
results and the documented CAS APIs are cited in the article.
